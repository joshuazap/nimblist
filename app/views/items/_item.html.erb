<% if current_user.items.any? %>
  <% current_user.items.each do |item| %> 
    <p><%= link_to "", item, class: 'glyphicon glyphicon-ok', method: :delete, remote: :true %> | <%= item.name %></p>
  <% end %>
<% end %>